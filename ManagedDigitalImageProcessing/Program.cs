namespace ManagedDigitalImageProcessing
{
    using System;

    /// <summary>
    /// The main class for the program.
    /// </summary>
    internal static class Program
    {
        /// <summary>
        /// The initial function called when the program starts.
        /// </summary>
        public static void Main()
        {
            Console.WriteLine(
                "All code filters are provided in the source code. The Program.Main function must be edited to trial the filters as no UI is available.");
            Console.WriteLine();
            Console.WriteLine("Press <Enter> to exit");
            Console.ReadLine();
        }
    }
}